#import json
#from zengarden_database import db, Mesh, Dialogue, Entities

#gar = {
    #'Water level': water_level,

#}
#jsonified_data = json.dumps(data1)
#json.loads(jsonified_data)

#-------------------------------------------
#mes = {
    #'Vertices' : vertices,
    #'Color' : colors,

#}
#jsonified_data = json.dumps(data2)
#json.loads(jsonified_data)

#-------------------------------------------

#dia = {
    #'Text' : text,
   # 'Option' : options,
    #'Font size' : font_size,
   # 'Time' : time,
    #'Weather' : weather_type,
    #'Day time' : day_time

#}
#jsonified_data = json.dumps(dia)
#json.loads(jsonified_data)

#-------------------------------------------

#ent = {
   # 'Position' : pos,
  #  'Mesh position' : mesh_ops,
    #'Garden id' : garden_id,
   # 'Mesh id' : mesh_id

#}
#jsonified_data = json.dumps(ent)
#json.loads(jsonified_data)
